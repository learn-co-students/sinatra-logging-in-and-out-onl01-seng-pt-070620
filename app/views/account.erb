<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
<% if Helpers.is_logged_in?(session) %>
    <a href="/logout">Log Out</a>
    <h1>Welcome <%= User.find_by(id: session[:user_id]).username %></h1>
    <h3>Your Balance: <%= User.find_by(id: session[:user_id]).balance.to_f %></h3>
  <% else %>
    You Must <a href="/">Log In</a> to View Your Balance'
  <% end %>

  </body>
</html>